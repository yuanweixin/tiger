let
    type list = {first: int, rest: list}
    var l := list{first=1, rest=nil}
    var m : list := nil
    var n := list{first=2, rest=list{first=39, rest=nil}}
in
    /* 1 + 2 + 39 = 42 */
    l.first + n.first + n.rest.first
end